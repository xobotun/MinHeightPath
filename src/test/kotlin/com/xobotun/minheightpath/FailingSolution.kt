package com.xobotun.minheightpath

/**
 * Just a solution to ensure tests are failing correctly
 */
class FailingSolution: Solution {
    override fun solve(mountainRange: List<Int>): List<List<Int>> {
        return listOf(emptyList())
    }
}
